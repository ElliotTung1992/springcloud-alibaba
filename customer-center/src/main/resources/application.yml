
server:
  port: 9002

spring:
  application:
    name: customer-center
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos

management:
  endpoints:
    web:
      exposure:
        include: '*'

service-url:
  nacos-user-center: http://user-center
